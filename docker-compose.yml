version: "3.8"

services:
  mysql: 
    image: mysql:8.0
    container_name: springboot-mysql
    restart: always

    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: devtiro_tasks
      MYSQL_USER: etv_dev
      MYSQL_PASSWORD: Etv@180203

    ports: 
      - "3307:3306"
    
    volumes: 
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: